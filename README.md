# TDS GA7 Release Gate

Deterministic policy endpoint that decides whether a GitHub Actions run may
promote a container image. See `release_gate.py` for the rules and
`test_release_gate.py` for the contract tests.
